function [sptr,sttr]=Sig_MK_month(ptr,ttr)
for i=1:12
    surf(result(:,:,i)');
    axis off;
    caxis([0,33])
%    colorbar;
    view([0,90]);
    grid off;
    strmax = ['Time = ',i];
    str=sprintf('%d-%d',year, month);
    title(str);
    saveas(gcf,[str 'cru_t' '.png']);

 
